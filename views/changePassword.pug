extends layouts/main.pug
 
block content
        .form-container
            form(action=routes.editProfile, method="post")
                input(type="text", placeholder="Current Password", name="currentPassword")
                input(type="text", placeholder="New Password", name="newPassword")
                input(type="text", placeholder="Verify Password", name="verifyPassword")
                input(type="submit", value="Change Password")