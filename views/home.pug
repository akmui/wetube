extends layouts/main.pug
include mixins/videoBlock.pug
 
block content
    .videos
        each item in videoDb
            +videoBlock({
                id: item.id,
                title: item.title,
                views:item.views,
                videoFile: item.videoFile
            })